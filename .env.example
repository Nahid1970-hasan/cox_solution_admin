
# Frontend dev server – IP and port (npm run dev). Use 0.0.0.0 to allow network access.
VITE_FRONTEND_HOST=127.0.0.1
VITE_PORT=5173

# Backend API – IP and port. All API requests (login, etc.) connect here (no trailing slash).
VITE_API_BASE_URL=http://127.0.0.1:8000
